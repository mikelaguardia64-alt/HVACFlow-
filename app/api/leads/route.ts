import {NextResponse} from "next/server"; import {createClient} from "@supabase/supabase-js"; export async function POST(req:Request){try{const body=await req.json(); if(!body.name||!body.email||!body.company)return NextResponse.json({error:"Missing required fields"},{status:400}); const url=process.env.NEXT_PUBLIC_SUPABASE_URL; const key=process.env.SUPABASE_SERVICE_ROLE_KEY; if(!url||!key)return NextResponse.json({error:"Database is not configured yet"},{status:503}); const db=createClient(url,key,{auth:{autoRefreshToken:false,persistSession:false}}); let {data:company}=await db.from("companies").select("id").eq("name",body.company).maybeSingle(); if(!company){const r=await db.from("companies").insert({name:body.company,email:body.email,phone:body.phone}).select("id").single(); if(r.error)throw r.error; company=r.data;} const r=await db.from("leads").insert({company_id:company.id,name:body.name,email:body.email,phone:body.phone,source:body.source||"website"}); if(r.error)throw r.error; return NextResponse.json({ok:true});}catch(e){return NextResponse.json({error:"Unable to capture lead"},{status:500});}}
"use client"; import {useState} from "react"; export default function Signup(){const [done,setDone]=useState(false); const [busy,setBusy]=useState(false); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/leads",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:f.get("name"),email:f.get("email"),phone:f.get("phone"),company:f.get("company"),source:"hvacflow-site"})});setBusy(false);setDone(r.ok); } return <main className="wrap"><div className="form"><div className="brand">HVAC<span style={{color:"#42d6ff"}}>Flow+</span></div><h1>Start your HVACFlow+ account</h1>{done?<><h2>You're on the list.</h2><p className="muted">Your onboarding request was captured successfully. We’ll use the information you provided to start setup.</p></>:<form onSubmit={submit}><label>Name</label><input className="input" name="name" required/><label>HVAC company</label><input className="input" name="company" required/><label>Email</label><input className="input" name="email" type="email" required/><label>Phone</label><input className="input" name="phone" required/><button className="btn" disabled={busy}>{busy?"Submitting…":"Start onboarding"}</button></form>}</div></main>}
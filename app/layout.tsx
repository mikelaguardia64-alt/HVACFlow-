import "./globals.css"; import type { Metadata } from "next"; export const metadata: Metadata={title:"HVACFlow+ | Turn missed calls into booked HVAC jobs",description:"AI-powered lead capture, missed-call recovery and appointment workflows for HVAC companies."}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
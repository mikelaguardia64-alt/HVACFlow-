"use client";import{useState}from"react";import{createBrowserClient}from"@supabase/ssr";const s=createBrowserClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!);export default function LoginPage(){const[e,setE]=useState(""),[p,setP]=useState(""),[m,setM]=useState(""),[b,setB]=useState(false);async function submit(x:React.FormEvent){x.preventDefault();setB(true);const{error}=await s.auth.signInWithPassword({email:e,password:p});if(error)setM(error.message);else location.href="/dashboard";setB(false)}return <main className="wrap"><div className="form"><div className="brand">HVAC<span style={{color:"#42d6ff"}}>Flow+</span></div><h1>Log in to HVACFlow+</h1><form onSubmit={submit}><label>Email</label><input className="input" type="email" required value={e} onChange={x=>setE(x.target.value)}/><label>Password</label><input className="input" type="password" required value={p} onChange={x=>setP(x.target.value)}/><button className="btn" disabled={b}>{b?"Signing in…":"Log in"}</button>{m&&<p className="muted">{m}</p>}</form><p><a href="/signup">Need an account? Sign up</a></p></div></main>}